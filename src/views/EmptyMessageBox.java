package views;

public class EmptyMessageBox {
/*    private Point point;

    private MessageBoxCtrlInterface messageBoxCtrl;
    private SimpleView<MessageBoxes> parent;

    public EmptyMessageBox(MessageBoxCtrlInterface messageBoxCtrl, Point startPoint, JPanel parent) {
        this.messageBoxCtrl = messageBoxCtrl;
        this.point = startPoint;
        this.parent = parent;
    }

    @Override
    public EmptyMessageBox drawView() {
        System.out.println(point.x + " " + point.y);
        EmptyBox emptyBox = new EmptyBox(point);
        parent.add(emptyBox, BorderLayout.CENTER);
        setVisible(true);
        return this;
    }

    @Override
    public void setVisible(boolean isVisible) {}*/
}
